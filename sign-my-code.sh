#!/usr/bin/bash

# install-gpg () {
#    if [ $(dpkg-query -W -f='${Status}' gpg 2>/dev/null | grep -c "ok installed") -eq 0 ];
#    then
#    sudo apt-get install gpg;
#    fi
# }


# install-gpg